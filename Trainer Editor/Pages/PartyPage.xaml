<Page x:Class="Trainer_Editor.Pages.PartyPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:global="clr-namespace:Trainer_Editor"
      xmlns:local="clr-namespace:Trainer_Editor.Pages"
      xmlns:UserControls="clr-namespace:Trainer_Editor.UserControls"
      mc:Ignorable="d" 
      d:DesignHeight="630" d:DesignWidth="1120"
      Title="PartyPage">

    <Grid>
        <Grid.Resources>

            <BitmapImage x:Key="GhostBitmap" UriSource="/Graphics/ghost.png"/>
            <BitmapImage x:Key="RocketBitmap" UriSource="/Graphics/rocket.png"/>
            <global:PartyTypeRadioButtonConverter x:Key="PartyTypeRadioButtonConverter"/>
            <Style TargetType="Image">
                <Setter Property="Height" Value="64"/>
                <Setter Property="Width" Value="64"/>
                <Setter Property="UseLayoutRounding" Value="True"/>
                <Setter Property="Source" Value="{Binding RelativeSource={RelativeSource Self}, Path=Tag.Species, 
                    Converter={StaticResource PokemonSpriteConverter}}"/>
            </Style>

        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.ColumnSpan="4" Orientation="Horizontal" Margin="10,10,10,10" >
            <RadioButton Content="NoItemDefaultMoves   " IsChecked="{Binding Path=SelectedTrainer.PartyType, Converter={StaticResource PartyTypeRadioButtonConverter}, ConverterParameter={x:Static global:PartyType.TrainerMonNoItemDefaultMoves}}"/>
            <RadioButton Content="ItemDefaultMoves   " IsChecked="{Binding Path=SelectedTrainer.PartyType, Converter={StaticResource PartyTypeRadioButtonConverter}, ConverterParameter={x:Static global:PartyType.TrainerMonItemDefaultMoves}}"/>
            <RadioButton Content="NoItemCustomMoves   " IsChecked="{Binding Path=SelectedTrainer.PartyType, Converter={StaticResource PartyTypeRadioButtonConverter}, ConverterParameter={x:Static global:PartyType.TrainerMonNoItemCustomMoves}}"/>
            <RadioButton Content="ItemCustomMoves   " IsChecked="{Binding Path=SelectedTrainer.PartyType, Converter={StaticResource PartyTypeRadioButtonConverter}, ConverterParameter={x:Static global:PartyType.TrainerMonItemCustomMoves}}"/>
            <RadioButton Content="Custom" IsChecked="{Binding Path=SelectedTrainer.PartyType, Converter={StaticResource PartyTypeRadioButtonConverter}, ConverterParameter={x:Static global:PartyType.TrainerMonCustom}}"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="1" Grid.ColumnSpan="3">
            <Image x:Name="TrainerSprite" Margin="10,0,0,0" 
                Source="{Binding SelectedTrainer.TrainerPic, Converter={StaticResource TrainerSpriteConverter},
                FallbackValue={StaticResource RocketBitmap}}"/>
            <Image Tag="{Binding SelectedTrainer.Party[0]}"/>
            <Image Tag="{Binding SelectedTrainer.Party[1]}"/>
            <Image Tag="{Binding SelectedTrainer.Party[2]}"/>
            <Image Tag="{Binding SelectedTrainer.Party[3]}"/>
            <Image Tag="{Binding SelectedTrainer.Party[4]}"/>
            <Image Tag="{Binding SelectedTrainer.Party[5]}"/>
        </StackPanel>
        

        <UserControls:PartyUserControl x:Name="party" Grid.Column="0" Grid.Row="2" Width="170" />

        <UserControls:MovesUserControl x:Name="moves" Grid.Column="1" Grid.Row="2" Width="150" VerticalAlignment="Center" 
            Style="{StaticResource PartyType}" Tag="{x:Static global:Input.Moves}"/>

            
        <StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Center">
            <UserControls:LevelOffsetUserControl Grid.Row="3" Tag="{x:Static global:Input.Custom}" Style="{StaticResource PartyType}"/>
            <UserControls:LabeledTextBox x:Name="lvlBox" LabelText="Level (1-100)"  Width="100"  Grid.Row="3" Margin="5,0,0,0"
                TextBoxText="{Binding SelectedMon.Lvl, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
        </StackPanel>
            
        <UserControls:AutoCompleteTextBox x:Name="heldItemBox" LabelContent="Held Item" Width="120" Grid.Row="3" Grid.Column="1"
            ListItem="{Binding SelectedMon.HeldItem, Mode=TwoWay}" ListSource="{Binding Items.List, Mode=OneWay}" 
            Tag="{x:Static global:Input.HeldItem}" Style="{StaticResource PartyType}"/>
    
        <UserControls:LabeledTextBox x:Name="ivBox" LabelText="IV (0-255)"  Width="100" Margin="5,0,0,0" Grid.Row="4"
            TextBoxText="{Binding SelectedMon.Iv, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <UserControls:IVsUserControl Grid.Row="5" Style="{StaticResource PartyType}" Tag="{x:Static global:Input.Custom}" Margin="0,5,0,0" HorizontalAlignment="Center"/>
        <UserControls:EVsUserControl Grid.Row="6" Style="{StaticResource PartyType}" Tag="{x:Static global:Input.Custom}" Margin="0,5,0,0" HorizontalAlignment="Center"/>

    </Grid>
</Page>
